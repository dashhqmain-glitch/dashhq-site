name: Temporary fire real call v2

on:
  workflow_dispatch: {}

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Show the-artifacts real collection data
        run: |
          curl -s "https://www.dashhq.site/toolkit/nft-collection?slug=the-artifacts" | jq '{name, floor, floorUsd, symbol, verified, category, twitter, discord, website}'
